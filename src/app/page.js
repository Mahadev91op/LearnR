import HeroSection from "../components/homepage/HeroSection";

export default function Home() {
  return (
    <main className="bg-black min-h-screen">
      <HeroSection />
      
      {/* Next components (Features, Pricing, etc.) hum yahan add karenge */}
    </main>
  );
}